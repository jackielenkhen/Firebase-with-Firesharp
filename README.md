# Firebase-with-Firesharp

this project with firebase realtime database 
+ Connection
+ Inset data
+ Push data
+ Update data
+ Remove data
+ listening all event like added,changed,removed (it not smoot)